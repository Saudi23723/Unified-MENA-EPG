name: Probe open-epg Qatar quality

# Research only, and deliberately not on main. Read-only, commits
# nothing, deleted once it has answered.
on:
  push:
    paths:
      - "tools/probe_openepg_qatar.py"
      - ".github/workflows/probe_openepg_qatar.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: python tools/probe_openepg_qatar.py
