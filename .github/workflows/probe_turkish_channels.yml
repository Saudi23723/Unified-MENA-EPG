name: Probe — TRT Spor, S Sport and HT Spor
on:
  push:
    paths:
      - "probes/probe_turkish_channels.py"
      - ".github/workflows/probe_turkish_channels.yml"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: python -u probes/probe_turkish_channels.py
