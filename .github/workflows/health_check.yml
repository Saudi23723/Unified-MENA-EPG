name: Health check

# Reads the published guides and reports what is wrong with them.
# It never writes, never commits and never touches a link — a red run
# here means a guide needs attention, not that anything was changed.

on:
  # Check after the merge has run, so a check can never catch the gap
  # between a source being rebuilt and the merged link catching up — which
  # is exactly what made this workflow red on 26 August, reporting two
  # channels "absent from the merged link" that were simply not merged yet.
  workflow_run:
    workflows: ["Merge Unified MENA EPG"]
    types: [completed]

  schedule:
    - cron: "25 5,17 * * *"
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "*.xml"
      - "health_check.py"
      - "merge_epg.py"
      - ".github/workflows/health_check.yml"

permissions:
  contents: read

concurrency:
  group: health-check
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Check every guide and the merged link
        run: python -u health_check.py
