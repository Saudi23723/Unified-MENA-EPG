# Ask what a WhatsApp Channel link actually serves a runner.
# Commits nothing, publishes nothing — it prints.
name: Probe WhatsApp Channel

on:
  workflow_dispatch:
  push:
    branches: [claude/arabic-channels-epg-99099k]
    paths:
      - "probes/probe_whatsapp_channel.py"
      - ".github/workflows/probe_whatsapp_channel.yml"

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
      - run: python -u probes/probe_whatsapp_channel.py
