# Ask FloGrappling where its schedule lives and what shape a row is.
# Commits nothing, publishes nothing — it prints.
name: Probe FloGrappling

on:
  workflow_dispatch:
  push:
    branches: [claude/arabic-channels-epg-99099k]
    paths:
      - "probes/probe_flograppling.py"
      - ".github/workflows/probe_flograppling.yml"

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
      - run: python -u probes/probe_flograppling.py
