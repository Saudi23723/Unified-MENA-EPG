name: Verify the guide shape
on:
  push:
    paths:
      - "tools/verify_guide_shape.py"
      - ".github/workflows/verify_shape.yml"
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: python -u tools/verify_guide_shape.py
